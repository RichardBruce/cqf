#include <iostream>

#include "grid_mapper.h"

int main()
{
    // grid<float> g(new uniformed_grid_mapper<float>(300.0, 0.0, 300));
    // g.dump();

    // for (int i = 0; i < 300; ++i)
    // {
    //     float coeffs[6];
    //     g.get_delta_coeffs(coeffs, i);
    //     g.get_gamma_coeffs(&coeffs[3], i);
    //     std::cout << coeffs[0] << " " << coeffs[1] << " " << coeffs[2] << " " << coeffs[3] << " " << coeffs[4] << " " << coeffs[5] << std::endl;
    // }

    
    return 0;
}